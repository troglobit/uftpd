EXTRA_DIST         = lib.sh ftp.sh tftp.sh mlst.sh maxfiles.sh
CLEANFILES         = *~ *.trs *.log

TEST_EXTENSIONS    = .sh
TESTS_ENVIRONMENT  = unshare -mrun

TESTS              = ftp.sh
TESTS             += tftp.sh
TESTS             += mlst.sh
TESTS             += maxfiles.sh
